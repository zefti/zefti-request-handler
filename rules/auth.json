{
  "password" : {
    "rules" : {
      "equivalence": "passwordVerify",
      "minLength": 8,
      "maxLength": 100,
      "type" : "string"
    },
    "required" : true,
    "alias" : "password"
  },
  "password_verify" : {
    "rules" : {
      "equivalence": "password",
      "minLength": 8,
      "maxLength": 100,
      "type" : "string"
    },
    "required" : true,
    "alias" : "passwordVerify"
  }
}